import React, { Fragment } from "react";

import UserInfo from "../components/UserInfo";

const Dashboard = () => {
  return (
    <Fragment>
      <UserInfo />
    </Fragment>
  );
};

export default Dashboard;
